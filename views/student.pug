extends layout

block content
  div#container
    div#timeline
      p.group-title #{Name}
      div.bio-image
        img(src="/images/"+(Image + 1)+".jpeg")
      h2 Cumple años el #{Born}
      h3 Le gusta:
      p #{Things}
